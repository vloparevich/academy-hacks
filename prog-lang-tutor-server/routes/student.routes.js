const router = require('express').Router();
const mongoose = require('mongoose');

// GET route to show the student
router.get('/', (req, res, next) => {});

router.post('/', (req, res, next) => {});

module.exports = router;
